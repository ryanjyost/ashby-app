{
  "name": "ashby",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "2.1.1"
  },
  "dependencies": {
    "antd": "^3.10.7",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-mailchimp-subscribe": "^2.0.2",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws s3 sync build/ s3://staging.ashby.app",
    "deploy-prod-dangerous": "aws s3 sync build/ s3://ashby.app",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
